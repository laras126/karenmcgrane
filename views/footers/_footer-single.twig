{% extends 'footers/footer.twig' %}

{% block footer_class %}single{% endblock %}

{% block footer_content %}
	{% if post.citations %}
		<h4>Citations</h4>
		<ul class="single__citations">
			{% for item in post.get_field('citations') %}
				{% set item = TimberPost(item) %}
				
				<li class="citations__list">
					<a href="{{item.source_url}}">{{item.post_title}}</a>
					{{ item.source_author ? 'By ' ~ item.source_author}}
					{{ item.source_publication ? item.source_publication}}
					{{ item.source_date ? item.source_date|date('| F j, Y') }}
				</li>
			{% endfor %}
		</ul>
	{% endif %}		

{% endblock %}